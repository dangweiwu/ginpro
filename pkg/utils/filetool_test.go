package utils

import (
	"fmt"
	"strings"
	"testing"
)

// func TestFile(t *testing.T) {
// 	fmt.Println(IsExists("h:/work"))
// 	IsExistAndCreateDir("./abc")
// }

func TestDemo(t *testing.T) {
	a := "template1"
	b := "temabate"
	fmt.Println(strings.Index(a, b))
}
